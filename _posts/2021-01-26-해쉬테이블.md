---
layout: single
title:  "해쉬테이블"
categories: [자료구조] # 홈페이지에서 카테고리를 통해 coding으로 지정되어있는 포스트들을 한번에 볼 수 있다
tag: [해쉬테이블]
permalink: /해쉬테이블/
toc: true
toc_sticky: true
author_profile: false # 왼쪽에 조그마한 글 나오는지 여부
sidebar:
    nav: "docs"     # Navigation에 있는 docs
# search: false # 만약 이 포스트가 검색이 안되길 원한다면 주석해제
---
- 시간복잡도
    - 삭제, 수정 모두 O(1)
- Key, value가 아니라 value만 사용
- 원리
    
    ![](/images/2022-01-26-02-49-54.png)

    - Array로 이루어져있으며 index를 Hash function을 통해 접근
        - 위 그림에서 글자수를 index로 반환해주는 Hash function이 있다고 하면 cake를 key로 줬을 때 바로 5달러로 접근 가능
        - taco를 넣게되면 기존에 저장되어있는 4로 접근하기 때문에 **Collision** 발생하며 이를 Hash collision이라고 함
            - 이것때문에 항상 O(1)로 삽입, 삭제, 탐색이 이루어지는것은 아니지만 평균적으로 따지기 때문에 O(1)로 생각하면 된다고 함. 평균적..?