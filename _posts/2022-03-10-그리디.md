---
layout: single
title:  "그리디"
categories: [코딩테스트] # 홈페이지에서 카테고리를 통해 coding으로 지정되어있는 포스트들을 한번에 볼 수 있다
tag: [코딩테스트, 그리디]
permalink: /그리디/
toc: true
toc_sticky: true
author_profile: false # 왼쪽에 조그마한 글 나오는지 여부
sidebar:
    nav: "docs"     # Navigation에 있는 docs
# search: false # 만약 이 포스트가 검색이 안되길 원한다면 주석해제
---

# 큰 수의 법칙

> P92쪽

- 내 코드


```python
N, M, K = map(int, input().split())

arr = list(map(int, input().split()))
print(arr)

max_val = max(arr)

arr.remove(max_val)
sec_max_val = max(arr)

quan = M // (K+1)
rest = M % (K+1)

final_val = (max_val * K + sec_max_val)*quan + max_val * rest
print(final_val)
```

- 피드백
  - input() 쪽 입력 처리가 익숙치 않아서 오래걸렸음. input().split()은 리스트를 반환하며 각 요소는 공백으로 구분된 String 타입임을 기억하자
  - if else로 max 값이 2개 이상 같을경우, 그렇지 않을경우로 나눠서 접근하려 했으나 list의 remove 이용하면 if else로 나눌 필요 없이 진행해도 된다는 것을 깨닫는데 오래걸림.
  - 연산자 우선순위를 고려하지 못해서 M // K + 1 의 문제점을 발견하지 못했음
  - max() 도 있지만 arr.sort() 이후 arr[-1], arr[-2] 이런식으로 접근해도 가장 큰 숫자, 두번째로 큰 숫자를 구할 수 있다.
